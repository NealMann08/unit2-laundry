# Copy to .env.local and set to point the app at a real server.
# Leave unset (or delete .env.local) to use the built-in simulator instead.
#
# No trailing slash. Use the machine's LAN IP rather than localhost if you
# want to open the page on your phone.
#
# VITE_API_URL=http://192.168.1.50:8000
